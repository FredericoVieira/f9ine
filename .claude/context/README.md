# .claude/context/

Detail that's too long for CLAUDE.md and only relevant to specific tasks — not loaded automatically, only when `/prime` or `code-reviewer` decides it's relevant.

One file per topic (`auth.md`, `testing.md`, etc). Two templates are included to start from — delete or replace them, they're placeholders, not required content.

Full picture of how this fits together: `.claude/HOW-THIS-WORKS.md`.
